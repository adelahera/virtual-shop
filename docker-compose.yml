# docker-compose.yml
version: '3.1'
services:
  app:
    build: ./e-commerce
    volumes:
      - ./e-commerce:/e-commerce
    depends_on:
      - mongo

  mongo:
    image: mongo
    ports:
      - "27017:27017"
    volumes:
      - ./data/db:/data/db  